Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass


.\venv\Scripts\Activate.ps1


python manage.py runserver

# لتشغيل المهام الخلفية (مثل توليد PDF) محلياً:
# افتح نافذة Terminal جديدة وشغل:
celery -A config worker --loglevel=info --pool=solo


-----------------------------


git add . 
git commit -m "الرفع55ريندر"
git push origin main


---------------------------------


تحديث قاعدة البيانات على ريندر 
python manage.py migrate

--------------------------------

git add .
git commit -m "🚀 إصلاح: إضافة psycopg لدعم PostgreSQL"
git push origin main

-------------------------------
ترحيل قاعدة البيانات:

python manage.py makemigrations
python manage.py migrate




----------------------------------

ENV=production
DEBUG=False
SECRET_KEY=*** ضع هنا المفتاح السري للإنتاج (من Render env vars) ***

DATABASE_URL=*** رابط قاعدة بيانات الإنتاج (من Render Postgres) ***

R2_ACCESS_KEY_ID=*** Cloudflare R2 Access Key ID ***
R2_SECRET_ACCESS_KEY=*** Cloudflare R2 Secret Access Key ***
R2_BUCKET_NAME=school-reports
# ملاحظة: ضع هنا endpoint بدون اسم الـ bucket
# مثال: https://<accountid>.r2.cloudflarestorage.com
R2_ENDPOINT_URL=*** Cloudflare R2 S3 API endpoint ***
# اختياري (إن لديك دومين عام أو r2.dev)
R2_PUBLIC_DOMAIN=*** public domain for media (optional) ***

ALLOWED_HOSTS=school-7lgm.onrender.com

----------------------------------------------------


ENV=development
DEBUG=True
SECRET_KEY=*** مفتاح سري للاستخدام المحلي فقط (غير مهم للإنتاج) ***

# SQLite محلي (التطوير)
# DATABASE_URL=sqlite:///db.sqlite3

# إذا حاب تجرب PostgreSQL محليًا
# DATABASE_URL=postgres://postgres:password@localhost:5432/school_db

R2_ACCESS_KEY_ID=*** Cloudflare R2 Access Key ID (dev) ***
R2_SECRET_ACCESS_KEY=*** Cloudflare R2 Secret Access Key (dev) ***
R2_BUCKET_NAME=school-reports
R2_ENDPOINT_URL=*** Cloudflare R2 S3 API endpoint (dev) ***
R2_PUBLIC_DOMAIN=*** public domain for media (optional) ***

ALLOWED_HOSTS=localhost,127.0.0.1

-----------------------------------------------

الفحص

python manage.py check



# تصميم و تطوير فهد الشهري 

--------------------------------------------------

pip install Pillow
python manage.py makemigrations
python manage.py migrate


python manage.py createsuperuser
