# الإصلاحات الحرجة — منصة توثيق

**المصدر:** [SECURITY_AUDIT_REPORT.md](SECURITY_AUDIT_REPORT.md)
**التاريخ:** 2026-08-11 · **آخر تحديث:** 2026-08-11 (بعد جولة التنفيذ)
**النطاق:** Critical و High فقط، إضافةً إلى Medium ذات الأثر المباشر على قرار الإطلاق.

---

## حالة التنفيذ

| البند | الحالة | الدليل |
|---|---|---|
| P0-1 · تدوير `SECRET_KEY` | ✅ **غير مطلوب — تُحقِّق على الإنتاج** | المفتاح الجاري (88 محرفًا) **ليس** أيًّا من الأربعة المسرَّبة، و`DATABASE_URL` كذلك. الحارس الآلي باقٍ لمنع الانحدار |
| P0-2 · إغلاق fail-open | ✅ **مُطبَّق** | `_login_account_locked` + `LOGIN_THROTTLE_FAIL_CLOSED` |
| P0-3 · وقف تسجيل PII | ✅ **مُطبَّق** | 6 أسطر سجلّ في `auth.py` |
| P0-4 · عمر الروابط الموقَّعة | ✅ **مُطبَّق** | `MEDIA_SIGNED_URL_EXPIRE_SECONDS=900` — **يحتاج اختبارك اليدوي قبل النشر** |
| P1-1 · تثبيت الاعتماديات | ✅ **مُطبَّق** | `requirements.lock.txt` + `Dockerfile` + بوّابة CI |
| P1-2 · فصل مخزن الحدود | ✅ **مُطبَّق** | `redis-limits` + `core/limits_cache.py` |
| P1-3 · ترويسات الحماية | ✅ **مُطبَّق** | `Caddyfile.fragment` — **يحتاج نشرًا للتفعيل** |
| P1-4 · احتفاظ سجل التدقيق | ✅ **مُطبَّق** | `AUDIT_LOG_RETENTION_DAYS=365` |
| P2-1 · توكن `moyasar_callback` | 🟡 **مؤجَّل عمدًا** | يكسر التفعيل التلقائي حتى تُحدَّث لوحة Moyasar |
| P2-2 · `Sec-Fetch-Site` | ✅ **مُطبَّق** | `mansour.py` |
| P2-3 · إزالة CDN من CSP | ✅ **مُطبَّق** | Chart.js مُستضاف محليًا |
| P2-4 · تفعيل Sentry | 🟠 **جاهز، ينقصه DSN** | مُنقّي PII (`before_send`) مُطبَّق، والسقالة القديمة حُذفت |

### أُضيف بعد ذلك — أتمتة الحراسة

| البند | الأثر |
|---|---|
| `core/compromised_secrets.py` | بصمات SHA-256 لكل سرّ ظهر في تاريخ Git (4 مفاتيح توقيع، 3 سلاسل قاعدة بيانات، 6 مفاتيح Cloudinary) |
| `production_preflight` — 3 أقسام جديدة | يفشل على: سرّ مسرَّب · fail-closed بلا مخزن مستقل · مخزن حدود غير قابل للكتابة · روابط > ساعة · وسائط عامة |
| `scripts/post_deploy_smoke.py` | يفحص الترويسات **الحقيقية** من الخارج — الشيء الوحيد القادر على كشف أن Caddy يُلغي إعداد Django |
| مُنقّي Sentry | ينزع كلمات المرور والتوكنات والجوال والهوية، **بما فيها متغيّرات الإطارات** |
| `LAUNCH_RUNBOOK.md` | ما تنفّذه أنت، وما يحرسه فحص آلي |

**التحقق:** 1549 اختبارًا — OK · `ruff` نظيف · `bandit` صفر عالي الخطورة · `pip-audit` على القفل: صفر ثغرات · `check --deploy --fail-level WARNING` نظيف.

---

## سُلّم الأولويات

| الرمز | المعنى |
|---|---|
| **P0** | يجب إصلاحه **قبل** الإطلاق التجاري |
| **P1** | يجب إصلاحه بأسرع وقت (خلال أسبوع من الإطلاق) |
| **P2** | تحسين مهم (خلال شهر) |
| **P3** | تحسين إضافي (حسب التوفّر) |

**لا توجد ثغرات Critical.** البنود أدناه هي High و Medium مرتّبة بأولوية التنفيذ.

---

## 🔴 P0 — قبل الإطلاق

### P0-1 · SEC-001 · التحقق من `SECRET_KEY` الإنتاجي وتدويره
**الخطورة:** High · **الوقت:** 30 دقيقة · **الخطر على الوظائف:** منخفض (يُسقط الجلسات النشطة فقط)

تاريخ Git يحتوي على `SECRET_KEY` إنتاجي و`DATABASE_URL` ومفاتيح Cloudinary عبر 19 commit. المستودع خاص، وهو ما يخفّض الخطورة، لكن **مفتاح توقيع Django مسرَّب يعني تزوير جلسات لأي مستخدم بلا كلمة مرور**.

**التنفيذ:**
```bash
# 1) استخرج المفتاح المسرَّب وقارنه بالحالي (على الخادم، لا في سجلّ)
git show 8785b272:.env | grep '^SECRET_KEY=' | sha256sum
grep '^SECRET_KEY=' deploy/hetzner/env.production | sha256sum
# التجزئتان متطابقتان ⇒ دوّر فورًا.

# 2) ولّد بديلًا
python -c "import secrets; print(secrets.token_urlsafe(64))"

# 3) حدّث env.production ثم
docker compose -f compose.hetzner.yaml up -d --force-recreate web worker-core worker-media beat
```

**تحقّق إضافي إلزامي:**
- [ ] هل مثيل PostgreSQL في `DATABASE_URL` المسرَّب ما زال حيًّا؟ إن نعم — دوّر كلمة مروره أو فكّكه.
- [ ] احذف مفاتيح Cloudinary من لوحة تحكم Cloudinary.

**معيار القبول:** تجزئة `SECRET_KEY` الإنتاجي لا تطابق أي قيمة في تاريخ Git.

---

### P0-2 · SEC-002 · إغلاق مسار الفشل المفتوح في حدّ الدخول
**الخطورة:** High · **الوقت:** 2 ساعة · **الخطر على الوظائف:** منخفض

`_login_account_locked` تعيد `False` عند فشل الكاش، و`volatile-lru` يُخلي عدّادات الإخفاق صامتًا تحت الضغط. النتيجة: **حماية التخمين تختفي بلا أثر، وقت الذروة تحديدًا**.

**التنفيذ:** راجع `Example Fix` في SEC-002 — يشمل:
1. `_login_account_locked` تفشل مغلقة + `logger.error` + عدّاد `opmetrics`.
2. مثيل `redis-limits` مستقل بسياسة `noeviction` لعدّادات الحدود.

**معيار القبول:** اختبار `test_login_is_blocked_when_cache_backend_is_down` يمرّ.

---

### P0-3 · SEC-003 · وقف تسجيل أرقام الجوال والهوية في السجلات
**الخطورة:** Medium (لكن التزام PDPL يجعله حاجزًا) · **الوقت:** 20 دقيقة · **الخطر على الوظائف:** صفر

ثلاثة أسطر في [reports/views/auth.py](reports/views/auth.py) تكتب المعرِّف الخام. الدالة الصحيحة موجودة أصلًا في نفس الملف ([auth.py:530](reports/views/auth.py#L530)).

**التنفيذ:** استبدل `identifier=%s` بـ `identifier_hash=%s` مع `_identifier_for_log()` في الأسطر 816 و820 و824.

**معيار القبول:** اختبار `test_failed_login_does_not_log_raw_identifier` يمرّ.

---

### P0-4 · SEC-005 · تقصير عمر روابط الوسائط الموقَّعة
**الخطورة:** Medium (لكنه المسار الوحيد المتبقّي لتجاوز العزل) · **الوقت:** 15 دقيقة + اختبار · **الخطر على الوظائف:** منخفض — **يتطلب اختبارًا يدويًا**

`AWS_QUERYSTRING_EXPIRE = 86400` ⇒ نافذة 24 ساعة لأي رابط مسرَّب.

**التنفيذ:**
```bash
# deploy/hetzner/env.production
AWS_QUERYSTRING_EXPIRE=900
```

**اختبار يدوي إلزامي قبل النشر:**
- [ ] تنزيل أرشيف ZIP لبيانات المدرسة
- [ ] فتح إيصال دفع من لوحة المنصة
- [ ] فتح مرفق تعميم وشاهد إنجاز
- [ ] تنزيل PDF ملف إنجاز

**معيار القبول:** الأربعة تعمل، واختبار `test_signed_media_urls_expire_within_one_hour` يمرّ.

---

## 🟠 P1 — بأسرع وقت (خلال أسبوع)

### P1-1 · SEC-004 · تثبيت شجرة الاعتماديات كاملة + بوّابة CI
**الخطورة:** Medium · **الوقت:** 3 ساعات

`requirements.txt` يثبّت المباشرة فقط. `pip-audit` على البيئة المحلولة يُظهر ثغرات معروفة في `urllib3`, `requests`, `twisted`, `idna`, `msgpack`, `pyasn1` — وكلها مسارات إنتاج فعلية عبر `boto3`/`daphne`/`channels-redis`.

**التنفيذ:** `pip-compile --generate-hashes` → `requirements.lock.txt` → `pip install --require-hashes` في `Dockerfile` → `pip-audit --strict` كبوّابة CI **داخل الصورة المبنية**.

**تنبيه:** لا ترقِّ الحزم دفعة واحدة قبل تحليل Breaking Changes. رقِّ `urllib3` و`requests` أولًا (منخفضة الخطورة)، ثم `twisted` منفصلًا (يمسّ daphne/WebSocket) مع اختبار WS كامل.

---

### P1-2 · SEC-002 (تكملة) · فصل Redis الحدود عن Redis الكاش
**الخطورة:** Medium · **الوقت:** 2 ساعة

المثيل الحالي يحمل الكاش والجلسات والطوابير والحدود معًا. `volatile-lru` يُخلي الحدود والجلسات تحت الضغط.

**التنفيذ:** خدمة `redis-limits` (راجع SEC-002)، وتوجيه `RATELIMIT_USE_CACHE` ومفاتيح `login:fail:*` إليها.

---

### P1-3 · SEC-006 · إكمال ترويسات الحماية وتصحيح HSTS
**الخطورة:** Low · **الوقت:** 30 دقيقة

`SECURE_HSTS_PRELOAD=True` في الكود **بلا أثر في الإنتاج** لأن Caddy يكتب الترويسة أولًا — إعداد يبدو كأنه يعمل وهو لا يعمل.

**التنفيذ:** أضف `Strict-Transport-Security` بـ `preload` و`Permissions-Policy` و`Cross-Origin-Resource-Policy` في `deploy/hetzner/Caddyfile.fragment`.

**تحقّق بعد النشر:**
```bash
curl -sI -A "Mozilla/5.0" https://tawtheeq-ksa.com/ | grep -iE "strict-transport|permissions-policy|cross-origin"
```

---

### P1-4 · SEC-010 · تمديد احتفاظ سجل التدقيق
**الخطورة:** Low · **الوقت:** 15 دقيقة

30 يومًا أقصر من دورة اكتشاف أي حادثة أمنية أو نزاع تجاري.

```bash
# deploy/hetzner/env.production
AUDIT_LOG_RETENTION_DAYS=365
```
مع أرشفة ما تجاوز 90 يومًا إلى R2 قبل الحذف لضبط حجم الجدول.

---

## 🟡 P2 — تحسين مهم (خلال شهر)

### P2-1 · SEC-008 · توكن مشترك لـ `moyasar_callback`
النقطة آمنة من ناحية سلامة البيانات (تعيد الاشتقاق من البوابة)، لكنها تسمح بتحفيز نداءات خارجية واستكشاف وجود مراجع. أضف توكنًا في المسار بمقارنة `hmac.compare_digest` — كما هو مطبَّق بالفعل في `tamara_webhook`. ووحّد الاستجابة إلى `200` دائمًا.

### P2-2 · SEC-009 · اشتراط نفس الأصل على `mansour_assistant_reply`
افحص `Sec-Fetch-Site` ضمن `{same-origin, same-site, none}` لمنع مواقع خارجية من استهلاك الميزانية عبر متصفحات الزوّار.

### P2-3 · SEC-007 · إزالة `cdn.jsdelivr.net` من `script-src`
استضف الأصول محليًا (يزيل الاعتماد الخارجي ويحسّن الأداء)، أو ثبّتها بـ SRI.

### P2-4 · تفعيل Sentry في الإنتاج
`SENTRY_DSN` فارغ حاليًا فالمراقبة معطّلة. `send_default_pii=False` مضبوط بالفعل — الإعداد جاهز، يلزم تفعيله فقط.

---

## ⚪ P3 — تحسين إضافي

### P3-1 · SEC-011 · TTL لمفاتيح إصدار اللوحة
`timeout=None` تجعلها غير قابلة للإخلاء تحت `volatile-lru`. الأثر الفعلي ≈ 400 KB عند 5,000 مدرسة — مهمَل. `timeout=30*24*3600` إن أُريد الاكتمال.

### P3-2 · SEC-012 · حذف نسخ SQLite من مجلد العمل
`db.sqlite3.backup-before-roles-migration` (17.8 MB) — احذفها بعد تأكيد نجاح الترحيل. استخدم بيانات مصطنعة للتطوير.

### P3-3 · تنظيف تاريخ Git من `.env`
`git filter-repo --path .env --invert-paths`. **يعيد كتابة كل الـ SHAs** — يحتاج تنسيقًا مع كل من يملك نسخة. التدوير (P0-1) يعالج الخطر الفعلي؛ هذا للنظافة.

### P3-4 · بيئة Staging لاختبار الحمل
غيابها منع تنفيذ اختبار حمل حقيقي، فتحليل السعة نظري من الإعدادات. بيئة مطابقة تتيح التحقق الفعلي من أرقام 1,000/2,000/5,000 متزامنًا.

---

## جدول التنفيذ الموصى به

| اليوم | البنود | الأثر |
|---|---|---|
| **1 صباحًا** | P0-1, P0-3 | إزالة خطر انتحال الهوية ووقف تسريب PII |
| **1 مساءً** | P0-2, P0-4 | إغلاق fail-open وتقليص نافذة تسريب الروابط |
| **1 مساءً** | تشغيل حزمة الاختبارات كاملة + الاختبار اليدوي لـ P0-4 | تأكيد عدم الانحدار |
| **← هنا يصبح القرار 🟢 READY FOR PRODUCTION** | | |
| **الأسبوع 1** | P1-1 → P1-4 | تصلّب سلسلة التوريد والبنية |
| **الشهر 1** | P2-1 → P2-4 | تحسينات دفاع في العمق |
| **حسب التوفّر** | P3-1 → P3-4 | نظافة وجاهزية تشغيلية |

---

## ملخص العدّ

| الخطورة | العدد | البنود |
|---|---|---|
| 🔴 Critical | **0** | — |
| 🟠 High | **2** | SEC-001, SEC-002 |
| 🟡 Medium | **3** | SEC-003, SEC-004, SEC-005 |
| 🔵 Low | **5** | SEC-006, SEC-007, SEC-008, SEC-009, SEC-010 |
| ⚪ Info | **2** | SEC-011, SEC-012 |
